---
applications:
 - name: php-redis-admin
   env:
    REDIS_NAME: {HUMAN_CLUSTER_NAME}
    REDIS_ADDRESS: {REDIS_ADDRESS}
    REDIS_PORT: {REDIS_PORT}
    REDIS_DATABASE: {DATABASE_AMOUNT}
    REDIS_PASSWORD: {PASSWORD}